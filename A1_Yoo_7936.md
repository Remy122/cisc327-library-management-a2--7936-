CISC 327 ASSIGNMENT 1: Remy Yoo
Student Number: 20407936
Group 5 - TA: Jasmin Zangeneh

Part 2:

| --------- | Function Name               | Implementation Status |                           Anything Missing?                           |
| --------- | --------------------------- | :-------------------: | :-------------------------------------------------------------------: |
| R1        | add_book_to_catalog         |        partial        | ISBN allows letters to be inputted when it should only take on digits |
| R2        | get_all_books               |       Complete        |                                  N/A                                  |
| R3        | borrow_book_by_patron       |        partial        |    users are allowed to borrow up to 6 books before error message     |
| R4        | return_book_by_patron       |        partial        |                       All features are missing                        |
| R5        | calculate_late_fee_for_book |        partial        |                       All features are missing                        |
| R6        | search_books_in_catalog     |        partial        |                       All features are missing                        |
| R7        | get_patron_status_report    |        partial        |                       All features are missing                        |

Part 3:

For each of the requirements I tested the following things:

For R1, I tested the following things:

- Adding a book with valid input
- Rejecting a book with a blank title
- Invalid ISBN
- A book with No input
- The amount of copies being a non positive number

For R2, I tested the following things:

- Displaying the catalog when books are present
- Displaying the catalog when there is no books present
- Making sure the catalog displays books in order
- Making sure the borrow button works

For R3, I tested the following things:

- Borrowing a book with valid parameters
- Borrowing a book with invalid parameters
- Unavaliable book scenario
- Testing the catalog to make sure books appear

From R4 onwards, none of the features can really be tested. However, Implemented
the following tests:

- Returning a book with valid entries
- Returning a book that was not borrowed by a user
- Returning a book with an invalid patron ID
- Returning a book with a late fee

For R5, I implemented the following tests:

- Calculating a late fee for a book returned on time
- Calculating a late fee for a book returned a day late
- Calculating a late fee for a book returned as late as possible
- Calculating a late fee for a book not borrowed by the user

For R6 I implemented the following tests:

- Finding a book by partial title match
- Finding a book by partial author match
- Finding a book by ISBN
- Finding a book with no results

For R7 I Implemented the following tests:

- patron status for user with borrowed books
- patron status for user with late fees
- patron status for user with no borrowed books
- patron status for user who has borrowed books in the past but none currently
